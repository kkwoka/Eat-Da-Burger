<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newEaten="{{devoured}}">
		{{!-- {{#if devoured }}EAT ME! {{/if}} --}}
		{{#if devoured }}{{else}}EAT ME!{{/if}}

	</button>
</li>